{
  "name": "@hexdeck/cli",
  "version": "0.1.4",
  "description": "Real-time dashboard for Claude Code sessions — monitor agents, track plans, detect collisions",
  "type": "module",
  "license": "MIT",
  "bin": {
    "hex": "dist/index.js"
  },
  "files": [
    "dist",
    "dashboard"
  ],
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "claude",
    "claude-code",
    "ai",
    "developer-tools",
    "cli",
    "dashboard",
    "inspector"
  ],
  "scripts": {
    "build": "tsup && rm -rf dashboard && cp -r ../local/out dashboard",
    "dev": "tsx src/index.ts",
    "prepublishOnly": "test -d dashboard && test -f dist/index.js || (echo 'Run npm run build first' && exit 1)"
  },
  "dependencies": {
    "hono": "^4.11.8",
    "@hono/node-server": "^1.19.9",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@hexdeck/server": "*",
    "@hexdeck/local": "*",
    "@types/node": "^25.2.1",
    "tsup": "^8.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
